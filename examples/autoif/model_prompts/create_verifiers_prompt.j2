You are an expert for writing evaluation functions in Python to evaluate whether a response strictly adheres to a constraint.

## Input data

Here is the constraint in {{ language }} language: 

{{ instruction }}

{% if placeholders %}
This constraint template contains the following placeholders that will be provided to the python function as keyword arguments at runtime:
{% for p in placeholders %}
- {{ p.name }}: {{ p.type }}. {{ p.description }}
{% endfor %}
{% endif %}

## Task specification

Please write a Python function named `evaluate` to evaluate whether an input string `response` adheres to the constraint. The function accepts keyword arguments (**kwargs) for any placeholder values that may be provided at runtime.

If the response adheres to the constraint, simply return True, otherwise return False. 

## Allowed python libraries

You are allowed to import ONLY the following libraries:
- re: Use when matching or searching text with regular expressions.
- json: Use when parsing or producing JSON-formatted data.
- csv: Use when reading or writing CSV-formatted data.
- string: Use when working with string constants (e.g., punctuation, digits, ascii letters).
- math: Use when performing mathematical operations (e.g., ceil, floor, sqrt, log).
- collections: Use when counting elements, ordering dicts, or using specialised containers like Counter and defaultdict.
- syllapy: Use when counting syllables in words.
- emoji: Use when detecting, counting, or converting emoji characters.
- dateutil: Use when parsing dates and times from various string formats.
- unicodedata: Use when inspecting Unicode character properties (e.g., category, name, normalisation).
- markdown: Use when converting Markdown text to HTML.
- bs4: Use when parsing and navigating HTML or XML documents.
- typing: Use when adding type hints to function signatures or variables.
- functools: Use when applying higher-order function utilities like lru_cache, reduce, or partial.

### NLP libraries

When the constraint requires NLP capabilities (e.g., POS tagging, sentence segmentation, dependency parsing, NER), multiple libraries below may be suitable. In such cases, randomly pick one of the applicable libraries. Do not combine multiple NLP libraries in a single function.

- spacy: Use when performing NLP tasks such as tokenisation, POS tagging, dependency parsing, or named entity recognition.{% if spacy_model %} When using spacy for {{ language }} text, load the model with `spacy.load("{{ spacy_model }}")`.{% endif %}
- stanza: Use when performing NLP tasks such as tokenisation, sentence segmentation, POS tagging, morphological analysis, lemmatisation, dependency parsing, or named entity recognition.{% if stanza_lang %} When using stanza for {{ language }} text, initialize the pipeline with `stanza.Pipeline('{{ stanza_lang }}', processors='tokenize,pos')` (add processors as needed: `lemma`, `depparse`, `ner`, `mwt`).{% endif %}
- trankit: Use when performing NLP tasks such as tokenisation, sentence segmentation, POS tagging, morphological tagging, dependency parsing, named entity recognition.{% if trankit_lang %} When using trankit for {{ language }} text, initialize the pipeline with `trankit.Pipeline('{{ trankit_lang }}')`.{% endif %}
{% if lang_code == 'en' %}- nltk: Use when performing NLP tasks such as sentence splitting, POS tagging, stemming, or accessing language corpora. NLTK has robust support for English text analysis.
{% endif %}


## Guidelines

- The input response string can be a multiline string - string containing newline characters. Consider this when writing your evaluation logic.
- The test cases must be designed to be complex and realistic, mirroring real-life scenarios. They must explicitly cover common edge cases or potential pitfalls of the instruction (e.g., punctuation, capitalization, varying formats, special characters, multiline strings, empty inputs, leading/trailing spaces, or near-miss scenarios). The 'output' for each case must be 100% correct and definitive, as these tests are the ultimate standard for cross-validation.
- Language localisation: The functions must take into account the specifics of the {{ language }} language and the test cases must be in the {{ language }} language.


## Output format:

Your response will only contain a single python code block with the function definition and a single json block with a list of {{ num_test_cases }} test cases in the key `cases`, which includes an input in the key `input` and an expected output in the key `output` (true, false). 

{{ examples }}
